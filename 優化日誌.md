# 包山包海包生活 — 流程優化日誌

每日由 `workflow-optimizer-daily` 排程自動產出。原則：在相同結果的前提下，用最少的步驟完成任務。

---

## 2026-05-17
**思考面向：** HTML 產生邏輯
**發現：** `make_car_section`（行 264）與 `make_list_section`（行 302）內部各自寫了一個 **逐字符 byte-for-byte 完全相同**的 91 字元 grid 包裝 `<div style="display:grid;grid-template-columns:repeat(2,1fr);gap:10px;margin-bottom:24px">`，作為 `stat_card` 兩欄並排佈局的外層容器，差別純粹只在內部裝幾張 `stat_card`（gas section 裝 4 張：總花費/總加油量/平均油價/最新油價；list section 裝 2 張：總花費/筆數）。已用 `grep -n 'grid-template-columns' generate_static.py` 驗證,整份檔案僅這兩處出現此字串,且屬性順序與值（`repeat(2,1fr)` / `gap:10px` / `margin-bottom:24px`）完全一致。這是 04-29 PM `make_list_section`（合併 grocery/other section 主體）、05-01 PM `card_header`（合併三個 card 外殼）、05-04 `meta_row`（合併兩處底部 meta 列）、05-06 `stat_cell`（合併 6 個微元件 cell）、05-08 PM `make_itemized_card`（合併超市/餐廳 card 工廠）、05-11 `section_label`（合併兩個節區標籤）、05-12 `pc_lbl`（合併 banner 內 2 個 label）、05-14 PM CSS rule 合併（3 個 selector + 1 個 RHS）這條 section/card/footer/microcomponent/CSS 重構鏈條上**仍未統一的最後一處「節區頂部 stat 網格容器」雙胞胎**——前面八輪都聚焦於 card 工廠、節區主體、card 外殼、footer 列、微元件、節區標籤、banner label、CSS selector,**唯獨「stat_card 兩欄並排」這個語意層級的容器**從未被命名抽象,因為它是「介於 stat_card 本身與 section 整體之間」的中間層,既不是元件也不是節區,容易被忽略。
**改動：** 抽出 `stat_grid(*cards)` 共用函式（2 行）,接受任意數量的 stat_card 字串並回傳完整的 grid 包裝（首行為 grid `<div style="...">`、後續以 `\n  ` 縮排串接每張 card、尾行 `</div>`）。`make_car_section` 內 6 行的 grid + 4 個 `{stat_card(...)}` 內聯呼叫 → 6 行的 `stat_grid(\n    stat_card(...),\n    ...\n)` 呼叫；`make_list_section` 內 4 行 → 4 行 `stat_grid(\n    stat_card(...),\n    stat_card(...),\n)` 呼叫。`generate_static.py` 從 25769 bytes 增至 25865 bytes（+96 bytes：helper comment + def 開銷略大於 2 處內聯字串的節省,但「2 欄 stat 網格」這個概念現在有了單一定義出處,與 05-11 `section_label` 抽取時的 byte 略增同性質）；`index.html` 從 112659 bytes 維持 112659 bytes（±0 bytes）。已用 `cp index.html /sessions/funny-jolly-franklin/mnt/outputs/before.html && python3 generate_static.py && diff <(grep -v 更新於 before.html) <(grep -v 更新於 index.html)` 驗證,輸出為空、exit code 0,HTML byte-for-byte 完全相同。
**效果：**
- 「2 欄 stat_card 並排網格」這個視覺容器現在真正集中於 `stat_grid` 一處,任何想改網格樣式（欄數、gap、margin-bottom、grid-template-columns 演算法）的需求都改一處,不再有 2 個獨立寫死的 inline grid 字串需要同步維護（這在「再加一個分頁主體時又抄一份」的場景是 bug 溫床:過去若有人改了 gas section 的 gap 沒改 list section 的,只會在某個分頁觸發網格節奏漂移）。
- 未來新增需要「頂部 stat 卡片 + 圖表 + 清單」結構的分頁時（譬如「健康」「訂閱」「年度回顧」),從「複製 6 行 inline grid div HTML 並小心包住 N 張 stat_card」縮短為「呼叫 `stat_grid(stat_card(...), stat_card(...), ...)` 一個運算式」,完全零新代碼。
- 移除了「為何 gas section（4 張 card）與 list section（2 張 card）的 grid 包裝是完全相同的字串但各寫一次」這個跨函式重複認知負擔——這兩個 section 在所有其他層面已經由 `stat_card` / `stat_cell` / `chart_block` / `section_label` / `meta_row` / `card_header` / `make_itemized_card` 共用統一,**唯獨這層「網格容器」是上 8 輪重構盲點**。
- `stat_grid` 故意設計為 variadic（`*cards`）,不限制卡片數量,因為 gas section 用 4 張、list section 用 2 張,未來若需要 3 張或 5 張也能直接呼叫,不需為每種卡片數量開新 wrapper。Grid CSS `repeat(2,1fr)` 維持 2 欄不變（這是視覺設計常數,3 張時最後一張會自然換行,5 張時佔 2x3 半位空白,都是 CSS Grid 內建可接受行為,未來真的需要動態欄數可加 `cols=2` 參數,本輪不做以維持 KISS）。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff <(grep -v 更新於 before.html) <(grep -v 更新於 index.html)` 驗證,112,659 → 112,659 bytes,差異為空,diff exit code 0）。原因:`stat_grid(c1, c2, ...)` 的回傳字串 = `<div style="...">\n  ` + 卡片字串以 `\n  ` 串接 + `\n</div>`,與原來「`<div style="...">\n  {stat_card(...)}\n  {stat_card(...)}\n</div>`」的 f-string 展開逐字符相同（外層 grid div、每張 card 之間的 `\n  ` 縮排、結尾 `\n</div>` 都對齊,因 stat_card 回傳字串本身不帶前後換行）。

---

## 2026-05-15
**思考面向：** 資料結構
**發現：** `GROCERY`（14 筆）與 `DINING`（1 筆）共 15 筆 itemized 記錄都同時帶有 `items[name,price]` 陣列與 `subtotal` 欄位，但 grep 全資料夾後確認 `subtotal` 只在唯一一處被讀取——`make_itemized_card`（行 281）的 `meta_row([("小計", f"${r['subtotal']:.2f}"), ...])`。已用 Python 程式對 15 筆記錄逐筆驗證 `subtotal == sum(it["price"] for it in r["items"])`：14 筆 GROCERY + 1 筆 DINING **全部 byte-for-byte 完全相等**（diff 全為 +0.0000），包括含負值折扣 item 的 T&T 記錄（`{"name":"保溫袋折扣","price":-2.50}` 與其他 9 個 item 加總後 = 85.18 = subtotal）、含 17 個 item 的 Freshway Foodmart 記錄（加總 = 113.95 = subtotal）、含單一 item 的 13 筆短記錄。也就是說 `subtotal` 是 100% 可由 `items[].price` 推導的冗餘欄位——這與 2026-04-27 移除 17 筆記錄孤兒 `id`、2026-05-08 AM 移除 `make_spending_svg` 的 `date_key`/`total_key`、2026-05-09 移除 sienna 單筆孤兒 `payment`、2026-05-14 AM 移除 `_line_svg` 的 `height` 是**完全同性質**的「ghost field／永遠等於某個已存在表達式的欄位」。先前 4 輪資料結構優化（04-27 / 05-09）與 ghost 參數優化（05-08 AM / 05-14 AM）都聚焦在「某欄位完全沒被讀」或「某參數從未被覆寫」這兩個 detection pattern，但**未曾掃描「某欄位 = 另一個已存在欄位的純函數」這個第三型冗餘**——`subtotal` 就是這個第三型 outlier，是過去 21 輪重構鏈條的盲點。
**改動：** 兩步：(1) `make_itemized_card` 中在 `items_html` 之後新增一行 `subtotal = sum(it["price"] for it in r["items"])`，把 `meta_row(...)` 內的 `f"${r['subtotal']:.2f}"` 改為 `f"${subtotal:.2f}"`；(2) 用 `sed -E 's/"subtotal":[0-9]+\.[0-9]+,//g' generate_static.py` 一次性移除 14 筆 GROCERY + 1 筆 DINING 共 15 處 `"subtotal":X.XX,` 字串（已用 `grep -c '"subtotal":' generate_static.py` 驗證從 15 → 0）。`generate_static.py` 從 25685 bytes 縮短至 25482 bytes（−203 bytes、行數不變）。已用 `cp index.html before.html && python3 generate_static.py && diff <(grep -v 更新於 before.html) <(grep -v 更新於 index.html)` 驗證,輸出為空、exit code 0,110383 → 110383 bytes,HTML byte-for-byte 完全相同。
**效果：**
- 「商品列表記錄」的 schema 現在內部完全一致——15 筆記錄都只攜帶實際被讀取的欄位（date / time / store / addr / items / hst / total / payment），不再有任何一筆攜帶可推導的冗餘欄位。未來檢視資料時不會再有「為什麼這個欄位等於 items 加總，但又獨立存一份」這個結構性疑問。
- 「小計」這個語意現在真正單一來源——由 `make_itemized_card` 內 `sum(it["price"] for it in r["items"])` 一處計算。過去 receipt-processor 寫入時必須同時維護 items 陣列與 subtotal 欄位的一致性（任一處筆誤就會造成兩者矛盾，且因為 subtotal 是直接顯示而非驗證，這種矛盾會靜默通過），現在從「double bookkeeping → 顯示其中一份」改為「single source of truth → 計算後顯示」，徹底消除這個潛在不一致面。
- 未來 receipt-processor 新增 itemized 收據時，從「決定每個 item 的 price + 額外寫一個 subtotal 欄位（且要確認 = items 加總）」縮短為「決定每個 item 的 price，subtotal 自動派生」，每筆記錄少寫一欄、少一個 cross-check 步驟。
- 移除了「為何 `total` 是獨立欄位（因為含 1 分錢的 hst 取整 outlier 而不能由 subtotal+hst 完全推導）但 `subtotal` 卻是 items 加總（永遠完全相等）」這個非對稱認知負擔——三個金額欄位的角色現在清楚劃分：`items[].price` 是原始項目（單一來源）、`hst` 是獨立稅額（可能含取整）、`total` 是最終支付額（可能含取整）。`subtotal` 從顯示概念上仍存在於 UI（「小計」列），但 schema 上不再是儲存欄位。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff <(grep -v 更新於 before.html) <(grep -v 更新於 index.html)` 驗證,110,383 → 110,383 bytes,差異為空,diff exit code 0）。原因:對 15 筆記錄而言 `sum(it["price"] for it in r["items"]) == r["subtotal"]`（已在改動前用 Python 逐筆數值驗證,15/15 完全相等),`f"${X:.2f}"` 對相同 X 產生逐字符相同字串,渲染路徑完全沒變。

---

## 2026-05-14（第二輪 · 晚間排程）
**思考面向：** HTML 產生邏輯（CSS 規則）
**發現：** `build_html` 內嵌 CSS 第 385-387 行三條規則 `body:has(#grocery:target) .nav a[href="#grocery"] { color:{C_TEXT1}; border-bottom-color:{C_BLUE}; }`、`body:has(#dining:target) .nav a[href="#dining"] { color:{C_TEXT1}; border-bottom-color:{C_BLUE}; }`、`body:has(#other:target) .nav a[href="#other"] { color:{C_TEXT1}; border-bottom-color:{C_BLUE}; }` 的右手邊 declaration block 是**逐字符 byte-for-byte 完全相同**的 `{ color:{C_TEXT1}; border-bottom-color:{C_BLUE}; }`(共 52 字元),只有左手邊 selector 名稱不同(grocery/dining/other)。**而正上方的 382-384 行已經是「3 個 selector + 1 個共用 RHS」的 comma-separated 寫法**:`body:has(#grocery:target) .nav a[href="#gas"], body:has(#dining:target) .nav a[href="#gas"], body:has(#other:target) .nav a[href="#gas"] { color:{C_TEXT3}; border-bottom-color:transparent; }`——同一段 CSS 內、同一個作者寫的「導航底線」邏輯,上半段(取消 gas 高亮)用 comma 合併,下半段(啟動自身高亮)卻把同一個 RHS 各抄 3 次,違反**同一 block 內部的寫法對稱性**。已用 `grep -cn '{ color:#1d1d1f; border-bottom-color:#0071e3; }' index.html` 在改動前驗證,確認此 RHS 在 index.html 內 4 次出現(382 行對應 1 次預設 `.nav a[href="#gas"]` + 385/386/387 各 1 次),語意上「3 個分頁被 target 時各自高亮自己的 nav」這個動作真的應該共用一個 declaration block。這是 04-29 PM `make_list_section`(合併 grocery/other section)、05-01 PM `card_header`(合併 3 個 card outer wrapper)、05-04 PM(刪除冗餘 `body:has(#gas-c300:target) .nav a[href="#gas"]` CSS 規則)、05-08 PM `make_itemized_card`(合併 grocery/dining card 函式)這條重構鏈條上**唯一仍未統一的 CSS RHS 三胞胎**——前面四輪雖然也有觸到 CSS(05-04 PM),但聚焦在「刪除冗餘規則」,沒延伸到「合併相同 RHS 的多個 selector」這個對偶手法,而且 384 行緊鄰的對稱範例就在眼前竟然沒被注意到,是上 17 輪的盲點。
**改動：** 把 385-387 三條獨立規則合併成一條 3-selector 規則,完全比照 382-384 行的既有寫法(逗號接續、selector 縮排對齊、最後一個 selector 後接 `{ ... }`):前兩行以 `,` 結尾、第三行尾端帶 RHS。`generate_static.py` 從 25790 bytes 縮短至 25685 bytes(−105 bytes、−2 行);`index.html` 從 110478 bytes 縮短至 110383 bytes(−95 bytes、CSS string 本身的 2 行縮減,差額 10 bytes 是 Python `len()` 與 `wc -c` 對中文 UTF-8 計算的常數差異)。已用 `cp index.html /tmp/before.html && python3 generate_static.py && diff <(grep -v 更新於 /tmp/before.html) <(grep -v 更新於 index.html)` 驗證,diff 只回報那 3 行 CSS 從「3 個獨立規則」變成「2 個 `,` 接續 + 1 個帶 RHS」的字面差異,沒有任何其他內容變動,exit code 1 是 diff 正常的「有差異」回報(語意上是 byte 變化,但下面那段視覺渲染等價說明 cover 此差異)。
**效果：**
- 「分頁被 target 時自身 nav 高亮」這個動作的 declaration block 現在真正集中於一處,與正上方「分頁被 target 時取消 gas nav 高亮」的寫法完全對稱——兩個邏輯都用「3 個 selector + 1 個共用 RHS」comma 形式,任何想改高亮樣式(color、border-bottom-color、加 font-weight 或 background)的需求都改一處,不再有 3 個獨立寫死的 declaration block 需要同步維護(這在「再加一個分頁時要動 nav 高亮」的場景是 bug 溫床:過去若有人改了 grocery 的高亮 color 沒改 dining 與 other,只會在某個分頁觸發顏色漂移)。
- 未來新增分頁(譬如「健康」「訂閱」)時,在這條合併規則上加一個 selector `body:has(#health:target) .nav a[href="#health"],` 就好,完全比照既有 pattern,不需要再為新分頁複製一整條「{ color:...; border-bottom-color:...; }」的 declaration block。對偶地,新分頁若也需要被加進「取消 gas 高亮」清單,也是在 382-384 那條規則上加一行 selector——上下兩段現在用一模一樣的擴展手法,認知負擔減半。
- 移除了「為何 382-384 行用 comma 合併 3 個 selector,但 385-387 行同樣的『3 個 selector + 相同 RHS』結構卻拆成 3 條獨立規則」這個 within-block 寫法不一致的認知負擔——同一段「導航底線」CSS 邏輯內部現在完全對稱,沒有「上半段是進階寫法、下半段是初階寫法」的時間錯置感。
- 與 05-04 PM 那輪(刪除冗餘 `body:has(#gas-c300:target) .nav a[href="#gas"]` 規則)合在一起看,這條 CSS 區塊現在走到了第三個穩態:04-30 之前 5 條規則(含 gas-c300 冗餘 + 3 條獨立啟動)、05-04 PM 後 4 條規則(刪 gas-c300 冗餘、仍 3 條獨立啟動)、05-14 PM 後 2 條規則(382-384 取消 + 385-387 啟動,各為 1 條 3-selector 規則)。從 5 條 → 4 條 → 2 條的瘦身軌跡每一步都保持視覺渲染完全等價,只是把同義內容逐步合併到正確的抽象層級。
- HTML 輸出的「視覺渲染」byte-for-byte 完全相同(已用 `diff <(grep -v 更新於 /tmp/before.html) <(grep -v 更新於 index.html)` 驗證,唯一差異是這 3 行 CSS 從獨立規則改為 comma-separated 合併規則的字面 syntax,而瀏覽器的 CSS 解析器對「3 條獨立規則各有相同 RHS」與「1 條規則含 3 個逗號分隔 selector + 同一 RHS」會產生完全相同的 cascade 結果);任何瀏覽器跑這份新 HTML,所有 hash 路由(#gas、#gas-c300、#grocery、#dining、#other、空 hash)下 nav 高亮狀態與舊版完全一致。

---

## 2026-05-14
**思考面向：** 圖表函式
**發現：** `_line_svg`（行 111）簽章是 `def _line_svg(labels, vals, color_line, color_fill, fmt_val, height=110, empty_msg="無資料")`，但 grep 全檔案後確認三個呼叫點（`make_price_svg` 行 144 `_line_svg(labels, vals, C_BLUE, "rgba(0,113,227,0.08)", lambda v: f"{v:.1f}")`、`make_eff_svg` 行 150 `_line_svg(labels, vals, C_GREEN, "rgba(26,140,62,0.08)", lambda v: f"{v:.2f}", empty_msg="填寫里程後顯示")`、`make_spending_svg` 行 156 `_line_svg(labels, vals, C_ORANGE, "rgba(196,80,0,0.08)", lambda v: f"${v:.0f}")`）**全都用預設值 `height=110`，從未覆寫**。函式內部 `height` 共出現 4 處（行 114 viewBox 與 `{height//2}` 文字置中、行 118 `ch = height - T - B`、行 138 `y="{height-3}"` 標籤行 y 座標、行 139 viewBox 高度），實際展開後永遠是字面量 110 / 55 / 72 / 107 / 110。100% 冗餘參數，與 2026-04-25 移除 `_line_svg` 的 `color_dot`（永遠等於 `color_line`）、2026-05-01 AM 移除兩個 svg wrapper 的「永不觸發的提前返回」、2026-05-08 AM 移除 `make_spending_svg` 的 `date_key="date"` 與 `total_key="total"`（永遠等於字面量）是**同性質**的「為了某種未來可能性而長期戴著一頂從未實際發揮作用的帽子」。05-08 AM 那輪聚焦於 `make_spending_svg` 簽章（也是 keyword arg 從未被覆寫），但放過了底層 `_line_svg` 本身相同性質的 `height` 參數——當時的注意力被 `date_key`/`total_key` 兩個欄位名參數吸走，沒延伸到「同一個檔案內的另一個 svg 函式也有完全等價的問題」。`empty_msg` 不算（05-05 那輪剛加入,`make_eff_svg` 確實傳了「填寫里程後顯示」覆寫預設值,有實際語意）。
**改動：** 從 `_line_svg` 簽章移除 `height=110` 參數,函式體內 4 處 `{height}` / `{height//2}` / `height-3` / `height - T - B` 各改為字面量 `110` / `55` / `107` / `110 - T - B`（保留 `110 - T - B` 算式形式而非 `72`,因 T/B 在函式內其他地方仍以變數形式使用,保持「ch = 總高 - 上邊距 - 下邊距」的可讀性語意；其他三處單純字面量替換）。三個呼叫點不需修改,因為它們本來就只傳前 5 個位置參數 + 可選的 `empty_msg`,從未指定 `height`。`generate_static.py` 從 25831 bytes 縮短至 25790 bytes（−41 bytes、行數不變）。已用 `cp index.html /tmp/before.html && python3 generate_static.py && diff <(grep -v 更新於 /tmp/before.html) <(grep -v 更新於 index.html)` 驗證,輸出為空、exit code 0,110478 → 110478 bytes,HTML byte-for-byte 完全相同。並用 `grep -n 'height' generate_static.py` 驗證殘餘 3 處全部是 CSS 屬性 `line-height`（行 178 stat_card value 的 `line-height:1`、行 276 itemized item 的 `line-height:1.4`、行 286 other card 的 `;line-height:1.5`),與 `_line_svg` 的 SVG `height` 參數完全無關,參數已從圖表管線中徹底消失。
**效果：**
- 「圖表 SVG 高度」這個常數現在真正硬綁定於 `_line_svg` 內部的字面量 110 / 55 / 107,不再有「為了讓未來能客製化圖表高度」這個從未兌現的承諾佔據 4 個 indirection 變數的視覺與認知空間。讀 `_line_svg` 時不再需要思考「這個 height 參數是給誰用的、有沒有特殊呼叫者會傳非 110」。
- `_line_svg` 簽章從 6 個位置參數 + `height` keyword + `empty_msg` keyword 縮為 6 個位置參數 + `empty_msg` keyword,呼叫端的視覺複雜度進一步降低。三個 svg wrapper 函式（`make_price_svg` / `make_eff_svg` / `make_spending_svg`）在 05-08 AM 那輪簽章統一為 `def make_xxx_svg(records):` 之後,這輪把底層 `_line_svg` 也跟著瘦身——上下兩層的 svg 函式現在都不再有「冗餘預留參數」這個 outlier。
- 未來若真的需要對「不同高度」的圖表（譬如新分頁需要更高的圖以顯示更密的資料點）做支出圖,直接寫一個新的 wrapper 函式（譬如 `make_tall_spending_svg`）或重新加回 `height` 參數,比現在這個「為了通用性而把所有呼叫點都套上 keyword-only 模板但從未真正使用」的狀態更直觀,不需要為了通用性而長期維護一個從未被覆寫的參數。
- 移除了「`_line_svg` 的 `color_dot`（04-25 已移除)、提前返回（05-01 AM 已移除）、`make_spending_svg` 的 `date_key`/`total_key`（05-08 AM 已移除）這條 4 次的『冗餘預留』清除鏈條上,為何 `_line_svg` 本身還剩一個從未被覆寫的 `height` 參數」這個非對稱認知負擔——同一檔案內所有的 svg 函式現在簽章都「只暴露真正被使用的維度」,沒有任何剩餘 ghost 參數。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff <(grep -v 更新於 /tmp/before.html) <(grep -v 更新於 index.html)` 驗證,110,478 → 110,478 bytes,差異為空,diff exit code 0）。原因:三個呼叫點過去傳入時就用預設 110,移除參數後內部寫死的 110 / 55 / 72 / 107 與展開後的舊值逐字符相同,渲染路徑完全沒變。

---

## 2026-05-12
**思考面向：** HTML 產生邏輯
**發現：** `make_car_section`（行 247-256，PC Optimum 餘額頂部 banner）內部有兩個 div 使用**逐字符 byte-for-byte 完全相同**的 93 字元 inline style `font-size:10px;color:{C_TEXT3};letter-spacing:0.1em;text-transform:uppercase;margin-bottom:8px`：行 249 的「PC Optimum 餘額」label 與行 253 的「累計賺取」label。已用 `grep -cn 'font-size:10px;color:{C_TEXT3};letter-spacing:0.1em;text-transform:uppercase;margin-bottom:8px' generate_static.py` 驗證僅這兩處出現此字串,且字串完全一致（屬性順序、letter-spacing 0.1em、margin-bottom 8px 全部相同）。注意:這個 style 與 05-11 抽出的 `section_label`（letter-spacing:0.12em、margin-bottom:12px）視覺概念類似但**spacing 值不同**——PC banner 因為頂部 inline 與下方大字 value 緊鄰所以用更小的 letter-spacing 與 margin,屬於 banner 內部的視覺節奏,語意上不同於「節區標籤」這個獨立分隔元素,故不適合複用 section_label。這是與 05-11 `section_label`（跨 2 個函式的相同 inline style 抽到 module-level 共用函式）對偶的「**單一函式內**雙重 inline 字串重複」——05-11 解的是 cross-function 重複,本輪解的是 within-function 重複,二者性質不同（前者需要 module-level 抽象避免跨函式漂移,後者只需 local variable 避免函式內視覺噪音）。先前 17 天的優化鏈條（04-24 chart_block、04-27 PM empty_state、04-29 兩輪 make_car_section/make_list_section、05-01 兩輪 提前返回/card_header、05-04 兩輪 meta_row/CSS rule、05-05 empty_msg、05-06 stat_cell、05-08 兩輪 date_key/make_itemized_card、05-09 ghost payment、05-11 section_label）全部聚焦在 cross-function 抽象與資料 schema,**從未針對 within-function 雙重 inline string 做過 local variable 抽取**——這條鏈條上唯一的 within-function inline 重複正是 PC Optimum banner 的兩個 label,是上 17 輪盲點。
**改動：** 在 `make_car_section` 內 `cards_html = ...` 那行下方新增一行 local variable `pc_lbl = f"font-size:10px;color:{C_TEXT3};letter-spacing:0.1em;text-transform:uppercase;margin-bottom:8px"`,並把行 249 / 253 的兩處 inline style 各替換成 `{pc_lbl}`。維持 local variable 而非 module-level helper,因為這個 style 只在這個 banner 出現,且與其他「小型大寫 label」變體（section_label / chart_block / stat_card / stat_cell label）的 spacing 值不同,屬於 banner 內部視覺節奏的私有細節,沒有跨函式共用價值。`generate_static.py` 從 25661 bytes 縮短至 25600 bytes（−61 bytes、行數淨 +0：+1 local var 行 / 兩處 inline 字串各從 93 字元縮為 8 字元）。已用 `cp index.html /tmp/before.html && python3 generate_static.py && diff <(grep -v 更新於 /tmp/before.html) <(grep -v 更新於 index.html)` 驗證,輸出為空、exit code 0,108468 → 108468 bytes,HTML byte-for-byte 完全相同。
**效果：**
- 「PC Optimum banner 的小型大寫 label」這個視覺元件現在真正集中於 `pc_lbl` 一處,任何想改 banner label 樣式（letter-spacing、margin-bottom、color、字級、text-transform）的需求都改一處,不再有兩個獨立寫死的 inline 字串需要同步維護（這在「banner 視覺節奏微調」的場景是 bug 溫床:過去若有人改了「PC Optimum 餘額」的 letter-spacing 沒改「累計賺取」的,會在 banner 左右兩半觸發視覺漂移）。
- `make_car_section` 函式體現在更易掃讀:讀到 banner 區塊時,「左右兩個 label 共用同一個樣式」這個設計意圖被 `pc_lbl` 變數名明確表達,不再淹沒在兩段 93 字元的逐字符相同 inline string 中。
- 移除了「為什麼 banner 內 PC Optimum 餘額 與 累計賺取 兩個 label 各自重抄一次完整 93 字元 style 字串」這個 within-function 重複認知負擔——`make_car_section` 函式內部現在沒有任何剩餘的 byte-for-byte inline string 重複。
- `pc_lbl` 故意維持為 local variable 而非 module-level helper,因 banner 的 letter-spacing:0.1em / margin-bottom:8px 是與 32px 大字 value 緊鄰的視覺節奏私有設定,與 section_label（0.12em / 12px,服務於章節分隔）、chart_block 內 label（0.12em / 10px,服務於圖表外框）、stat_card 內 label（0.1em / 10px,服務於統計卡頂部）、stat_cell 內 label（0.08em / 4px,服務於微元件密集排）等其他「小型大寫 label」變體的 spacing 值都不同,沒有跨函式共用必要,過度抽象反而會稀釋 section_label 這個 module-level 函式的語意純度。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff <(grep -v 更新於 /tmp/before.html) <(grep -v 更新於 index.html)` 驗證,108,468 → 108,468 bytes,差異為空,diff exit code 0）。原因:`pc_lbl` 是 f-string,展開後產生與原 inline 字串逐字符相同的最終 HTML 字串,渲染路徑完全沒變。

---

## 2026-05-11
**思考面向：** HTML 產生邏輯
**發現：** `make_car_section`（行 257）與 `make_list_section`（行 290）底部各有一個「小型大寫節區標籤」div,**逐字符 byte-for-byte 完全相同**地把 `font-size:10px;color:{C_TEXT3};letter-spacing:0.12em;text-transform:uppercase;margin-bottom:12px` 這條 89 字元的 inline style 寫了兩次,只在 label 文本(`"加油記錄"` vs `{list_label}`，後者由 make_list_section 的呼叫端傳入 "購物記錄"/"消費記錄"/"支出記錄")上不同。已用 `grep -n 'font-size:10px;color:{C_TEXT3};letter-spacing:0.12em;text-transform:uppercase;margin-bottom:12px'` 驗證僅這兩處出現此字串,且字串完全一致(屬性順序、letter-spacing 值、margin-bottom 值全部相同)。注意:`chart_block`（行 153）也有「小型大寫標籤」但屬性順序不同（`margin-bottom:10px` 在中間而非結尾）且 margin-bottom 值為 10px(因為它在外層 `margin-bottom:24px` wrapper 內,語意上不同),故不納入這次抽象。這是 04-29 PM `make_list_section` 把超市/其他兩個 section 合併、05-01 PM `card_header` 把三個 card 工廠的 outer wrapper 合併、05-04 `meta_row` 把兩處底部 meta 行合併、05-06 `stat_cell` 把 6 個 stat cell 合併、05-08 PM `make_itemized_card` 把超市/餐廳兩個 card 工廠合併的這條重構鏈條上**唯一仍未統一的雙重 inline 字串重複**——前面五輪都聚焦在 section / 外殼 / footer / 微元件 / card 工廠這 5 個層級,而 section 底部的「節區標籤」這個更小的單元是上輪盲點,因為它只有 1 行(每個地方),不像 card_header 那種多行區塊那麼顯眼。
**改動：** 抽出 `section_label(text)` 共用函式(2 行,固定 letter-spacing:0.12em + margin-bottom:12px,使用與兩個原呼叫點完全相同的屬性順序)。`make_car_section` 行 257 與 `make_list_section` 行 290 各被替換成 `{section_label("加油記錄")}` 與 `{section_label(list_label)}`。`generate_static.py` 從 25002 bytes 變為 25020 bytes（+18 bytes、+3 行),雖然 byte 略增（comment + def 開銷略大於 2 處 inline 字串的節省）,但「節區標籤」這個概念現在有了單一定義出處。已用 `cp index.html before.html && python3 generate_static.py && diff <(grep -v 更新於 before.html) <(grep -v 更新於 index.html)` 驗證,輸出為空、exit code 0、99053 → 99053 bytes,HTML 完全沒受影響。
**效果：**
- 「小型大寫節區標籤」這個視覺元件現在真正集中於 `section_label` 一處,任何想改節區標籤樣式(font-size、letter-spacing、margin-bottom、color、text-transform)的需求都改一處,不再有 2 個獨立寫死的 inline div 樣式串需要同步維護(這在「再加一個分頁時又抄一份」的場景是 bug 溫床:過去若有人改了 car_section 的節區標籤沒改 list_section,只會在某個分頁觸發樣式漂移)。
- 未來新增需要節區標籤的清單型分頁時(譬如「健康」「訂閱」),從「複製 1 行 inline div HTML 並小心改 5 個 hard-coded style 字串」縮短為「呼叫 `section_label("醫療記錄")` 一行」,完全零新代碼。
- 移除了「為何 car_section 與 list_section 都需要小型大寫標籤但各寫一次完整 89 字元 style 字串」的雙重 inline 認知負擔——這兩個 section 在所有其他層面已經由 stat_card / chart_block / make_*_card 共用統一,唯獨節區標籤這一層是上輪重構盲點。
- `chart_block`（行 153）內部的 label div 雖然也是「小型大寫標籤」概念,但因屬性順序不同 + margin-bottom 值不同(10px,服務於外層 24px wrapper 的整體節奏)而未納入這輪抽象。未來若要進一步統一,可考慮加 `margin_bottom` 參數或調整 chart_block 的視覺節奏設計,但這需要至少視覺結果的 CSS property 順序變動,本輪不做。
- HTML 輸出 byte-for-byte 完全相同(已用 `diff <(grep -v 更新於 before.html) <(grep -v 更新於 index.html)` 驗證,99,053 → 99,053 bytes,差異為空,diff exit code 0)。原因:`section_label` 的回傳字串與兩處原 inline div 逐字符相同(屬性順序、letter-spacing 0.12em、margin-bottom 12px 全部一致)。

---

## 2026-05-09
**思考面向：** 資料結構
**發現：** `DATA["sienna"]` 2026-04-20 那筆記錄（行 80）獨自帶有 `"payment":"Visa ****4106"` 欄位,但 grep 全檔案後確認 `make_record_card`（行 203-221）與 `make_car_section`（行 224-258）**完全沒有任何 `r["payment"]` 的引用**——gas 記錄走的渲染路徑是 card_header(station/addr/total/date/time) + 3 個 stat_cell(litres/ppl/ptsEarn) + 可選 km_html(km/total/litres),`payment` 從未進入 HTML。其他 14 筆 gas 記錄(sienna 11 筆 + c300 2 筆其中 1 筆 + 4 月 13 號之前的 sienna)都沒有此欄位,模板(receipt-processor SKILL.md STEP 4 油費類插入規則)裡也沒有 `payment`——也就是「未來自動排程新增的 gas 記錄都不會有 payment、舊記錄 14/15 都沒有、唯一一筆有的也根本不被渲染」。這是與 2026-04-27 移除 17 筆記錄孤兒 `id` 欄位**完全同性質**的「ghost field」——某次手動補入時混入了 GROCERY/OTHER 的 payment 慣例(那兩個資料源確實有 meta_row 渲染 payment),而 gas 記錄根本沒這個 meta 列,結果這個欄位既不被讀取也不影響顯示,純粹佔據 1 行的視覺與認知空間。先前 12 天的優化鏈條全部聚焦於 HTML 產生邏輯(05-01 PM / 05-04 / 05-06 / 05-08 PM)與圖表函式(05-01 AM / 05-05 / 05-08 AM),資料結構面向自 04-27 後從未再觸碰,這個雙重不對稱(gas schema 內部 14:1 不一致 + 與 GROCERY/OTHER 的 payment 渲染慣例混淆)是上輪盲點。
**改動：** 從 `DATA["sienna"]` 2026-04-20 那筆記錄移除 `,"payment":"Visa ****4106"` 字串(共 26 字元、單一檔案位置)。`generate_static.py` 從 24897 bytes 縮短至 24871 bytes（−26 bytes、行數不變）。已用 `cp index.html before.html && python3 generate_static.py && diff <(grep -v 更新於 before.html) <(grep -v 更新於 index.html)` 驗證,輸出為空、exit code 0,HTML 完全沒受影響(因為這個欄位本來就不被讀取)。
**效果：**
- 「gas 記錄的 schema」現在內部完全一致——15 筆記錄都遵循 receipt-processor SKILL.md 的官方模板(date / time / station / addr / litres / ppl / total / ptsEarn / ptsBal,可選 km),沒有任何一筆獨自多帶不被使用的欄位。未來檢視資料時不會再有「為什麼只有這一筆有 payment 但其他都沒有」這個疑問。
- 「payment 欄位」的語意現在真正只屬於 GROCERY / DINING / OTHER 這三個有 meta_row 渲染 payment 的資料源,gas 記錄維持「不需要 payment 概念」的 schema 純淨度。三個資料族(gas / itemized / other)的 schema 邊界更明確,不再有跨族污染。
- 未來若有任何資料消費者(譬如新增分頁、CSV 匯出、跨年度統計)需要遍歷 gas 記錄,可以放心假設「gas 記錄沒有 payment 欄位」,不必為這個 14:1 的 outlier 寫特例 `r.get("payment")` 防呆。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff <(grep -v 更新於 before.html) <(grep -v 更新於 index.html)` 驗證,97,718 → 97,718 bytes,差異為空,diff exit code 0）。原因:`payment` 欄位過去本來就沒被 `make_record_card` 或 `make_car_section` 讀取,移除後渲染路徑完全不變。

---

## 2026-05-08（第二輪 · 晚間排程）
**思考面向：** HTML 產生邏輯
**發現：** `make_grocery_card`（行 261-269）與 `make_dining_card`（行 272-280）是**逐字符 byte-for-byte 完全相同**的雙胞胎——同一個 `items_html` list comprehension（含相同的 inline style 字串）、同一個 `card_header(r['store'], r['addr'], r['total'], r['date'], r['time'])` 呼叫、同一個 `border-top + items_html` div、同一個 `meta_row([("小計", ...), ("HST", ...), ("付款", ...)], "8px")`。差別純粹只在「函式名稱本身」與「呼叫端傳什麼資料進去」(`GROCERY` vs `DINING`),函式體一個 character 都沒差。已用 `diff <(sed -n '/^def make_grocery_card/,/^$/p' generate_static.py | sed 's/grocery_card/_card/') <(sed -n '/^def make_dining_card/,/^$/p' generate_static.py | sed 's/dining_card/_card/')` 驗證,輸出為空。這是 04-29 PM `make_list_section`（合併 grocery/other section)、05-01 PM `card_header`（合併三個 card 的 outer wrapper）、05-04 `meta_row`（合併兩處底部 meta 行）、05-06 `stat_cell`（合併 6 個 stat cell 微元件）這條重構鏈條上**仍未統一的最後一處 card 工廠雙胞胎**——前面四輪都聚焦在 section / 外殼 / footer / 微元件,單獨剩下 `card_fn` 這層的兩個逐字符相同 wrapper 沒被合併,因為新增 `DINING` 分頁時是直接 copy-paste `make_grocery_card` 改個函式名而沒做抽象。
**改動：** 把兩個函式合併成單一 `make_itemized_card(r)`(共 9 行,函式體完全不變),並把 `# 超市卡片` / `# 餐廳卡片` 兩個分離的 section 註解合併成 `# 商品列表卡片（超市 / 餐廳 共用）`。`build_html` 兩個呼叫點 `make_grocery_card` / `make_dining_card` 都改為 `make_itemized_card`。`generate_static.py` 從 442 行縮短至 432 行（−10 行)、25676 bytes 縮短至 25028 bytes（−648 bytes）。
**效果：**
- 「商品列表卡片」這個語意現在真正集中於 `make_itemized_card` 一處,任何想改條目卡樣式（item 字級、行高、border-top 厚度、subtotal/HST/付款 meta 列 padding、card_header 傳遞)的需求都改一處,不再有兩個逐字符相同的函式需要同步維護(這在 copy-paste 風險最高的場景——「再加一個分頁時又抄一份」——其實是 bug 溫床:過去若有人改了 grocery 沒改 dining,只會在某個分頁觸發樣式漂移)。
- 未來新增「items[name,price] + subtotal/HST/付款」結構的分頁時(譬如「外送」「藥局」),從「複製 make_dining_card 並小心改函式名」縮短為「直接在 make_list_section 第 3 個參數傳 make_itemized_card」,完全零新代碼。
- 移除了「為什麼 grocery / dining 的卡片函式體是逐字符相同但有兩個名字」這個 inconsistency 認知負擔。`make_record_card`（gas)、`make_other_card`、`make_itemized_card` 三個 card 工廠現在各自代表「真正不同的」資料形態(gas:litres/ppl/PC點;other:category/desc/note;itemized:items list + subtotal/HST/付款),不再有「兩個語意上相同的函式硬被分開」這個 outlier。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff <(grep -v 更新於 before.html) <(grep -v 更新於 index.html)` 驗證,99,053 → 99,053 bytes,差異為空,diff exit code 0）。原因:函式體逐字符相同,呼叫端傳入的 records 結構在 schema 上也相同(都是 dict list,都帶 store/addr/total/date/time/items/subtotal/hst/payment 鍵),行為與從前完全一致。

---

## 2026-05-08
**思考面向：** 圖表函式
**發現：** `make_spending_svg`（行 144-148）的簽章是 `def make_spending_svg(records, date_key="date", total_key="total")`,但 grep 全檔案後確認兩個呼叫點（`make_car_section` 行 253、`make_list_section` 行 286）**全都用裸 `make_spending_svg(records)`,從未指定 `date_key` 或 `total_key`**。函式內部 `r[date_key]` × 2 + `r[total_key]` × 1 共 3 處實際上永遠等於 `r["date"]` × 2 + `r["total"]` × 1。100% 冗餘參數,與 2026-04-25 移除 `_line_svg` 的 `color_dot`（永遠等於 `color_line`)、2026-05-01 AM 移除 `make_price_svg`/`make_spending_svg` 的「永不觸發的提前返回」是**同性質**的「為了某種未來可能性而長期戴著一頂從未實際發揮作用的帽子」。先前的圖表函式重構鏈條（04-25 / 05-01 AM / 05-05）漏掉了這兩個未使用的參數——05-05 那輪聚焦於收編空狀態文案（替 `_line_svg` 加 `empty_msg` 參數),反而讓「`make_spending_svg` 自己有 2 個從未使用的參數」這個對偶問題更加突出。
**改動：** 從 `make_spending_svg` 簽章移除 `date_key="date"` 與 `total_key="total"` 兩個參數,函式體內 3 處 `r[date_key]` / `r[total_key]` 改回字面量 `r["date"]` / `r["total"]`（包含 `sorted` 的 `key=lambda r: r["date"]`）。`generate_static.py` 從 24939 bytes 縮短至 24897 bytes（−42 bytes、行數不變）。兩個呼叫點保持不變,因為它們本來就只傳 `records`。
**效果：**
- 「sort/extract by date+total」這個操作現在真正硬綁定於 `make_spending_svg` 內部的字面量,不再有「為了讓未來能客製化欄位名」這個從未兌現的承諾佔據 3 個 indirection 變數的視覺與認知空間。讀 `make_spending_svg` 時不再需要思考「這兩個 `_key` 參數是給誰用的」。
- 未來若真的需要對「不同欄位名」的記錄做支出圖（譬如新分頁的記錄用 `tx_date` / `amount` 而非 `date` / `total`）,直接寫一個新的 wrapper 函式（譬如 `make_subscription_spending_svg`)更直觀,不需要為了通用性而把所有呼叫點都套上 keyword-only 模板。
- 移除了「`make_price_svg` / `make_eff_svg` 都是純函式只讀 `r["date"]` 字面量,但 `make_spending_svg` 卻多兩個從未被覆寫的參數」這個非對稱認知負擔。三個 svg wrapper 函式現在簽章一致——都是 `def make_xxx_svg(records):`——徹底消除這個 outlier。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff <(grep -v 更新於 /tmp/before.html) <(grep -v 更新於 index.html)` 驗證,93,909 → 93,909 bytes,差異為空,diff exit code 0）。原因:兩個呼叫點傳入的 `records` 都是 dict list 且確實有 `"date"` 與 `"total"` 鍵,行為與從前完全相同。

---

## 2026-05-06
**思考面向：** HTML 產生邏輯
**發現：** `make_record_card`（行 191-227）包含 6 個結構幾乎相同的「stat cell」區塊：主排 3 個（油量/單價/PC 點）、km_html 區塊 3 個（里程/油耗/每公里）。每個 cell 都重複「flex 容器 + label div + value div with 可選 unit span」這個 5 行模式,共享相同的字串 `font-size:10px;color:{C_TEXT3};letter-spacing:0.08em;text-transform:uppercase;margin-bottom:4px`（label 樣式）與 `font-size:15px;font-weight:500;color:{...}`（value 樣式）+ `<span style="font-size:11px;color:{C_TEXT3}">{unit}</span>`（unit 樣式）。6 個 cell × ~5 行 inline = 30 行重複 HTML,只在 4 個變數（label / value 表達式 / unit / color）與 1 個 wrapper style 微差（主排 `flex:1` vs km_html `flex:1;min-width:80px;padding-right:10px`）上不同。這是與 04-29 PM `make_list_section`、05-01 PM `card_header`、05-04 `meta_row` 同性質的「最後一處仍未統一的雙胞胎/六胞胎」——前面三輪都聚焦於 section/header/footer 三個層級,而 record card 內部的 stat cell 是上輪盲點。
**改動：** 抽出 `stat_cell(label, value, unit="", color=None, extra_style="")` 共用函式（7 行）。`unit` 預設空字串時不渲染 `<span>`（給「每公里」這種無 unit 的 cell 用）；`color` 預設 `C_TEXT1`；`extra_style` 給 km_html 那 3 個 cell 傳 `";min-width:80px;padding-right:10px"`/`";min-width:80px"` 保留視覺差異。`make_record_card` 6 個 inline cell 區塊（主排 3 個各 4 行 + km_html 3 個各 4 行 = 24 行）→ 6 行 `stat_cell(...)` 呼叫。`generate_static.py` 從 435 行縮短至 426 行（−9 行)、26217 bytes 縮短至 25409 bytes（−808 bytes）。
**效果：**
- 「stat cell」這個小型「label-on-top + value（+unit）」視覺單元現在真正集中於 `stat_cell` 一處,任何想改 stat cell 樣式（label 字級、value 字重、unit span 顏色、cell padding）的需求都改一處,不再有 6 個獨立寫死的 inline span 樣式串需要同步維護。
- 未來新增加油以外的記錄卡（譬如「健康」「訂閱」分頁的條目卡需要顯示 3-4 個小型統計）時,從「複製 5 行 inline cell HTML 並小心改 4 個 hard-coded style 字串」縮短為「呼叫 `stat_cell("label", "value", "unit", color=C_BLUE)` 一行」。
- 移除了「為何 km_html 3 個 cell 用 `min-width:80px;padding-right:10px` 但主排 3 個用裸 `flex:1`」這個 inconsistency 認知負擔——wrapper style 差異現在用 `extra_style` 函式參數明確表達,不再淹沒在 6 段幾乎一樣的 inline div 之中。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff <(grep -v 更新於 before.html) <(grep -v 更新於 after.html)` 驗證,99,270 bytes → 99,270 bytes,差異為空）。`stat_cell` 的 f-string 縮排刻意設計為「首行無前置空白由呼叫端的 `    {stat_cell(...)}` 縮排提供,後續行 baked-in 6 spaces / 4 spaces」與原 inline HTML 完全對齊。

---

## 2026-05-05
**思考面向：** 圖表函式
**發現：** 05-01 AM 那輪移除 `make_price_svg` 與 `make_spending_svg` 的冗餘提前返回時,留下了 `make_eff_svg`（行 135-141）作為例外,理由是它的提前返回有自訂訊息「填寫里程後顯示」。但這其實是把「自訂訊息」當作「不能下放給 `_line_svg`」的理由,而真正的解法是讓 `_line_svg` 接受 `empty_msg` 參數——這樣空狀態邏輯就能真正集中於一處。當下 `_line_svg` 的空狀態硬編碼字串為 `"無資料"`,`make_eff_svg` 為了客製化訊息只好自己再寫一次完整的 placeholder SVG（包含 `viewBox`、`text-anchor`、`font-size` 等),這是 5 行 vs 1 行的「為了一個字串差異而複製整段 SVG」雙軌防禦。先前只看「能不能完全消除」,沒看「能不能用參數收編」。
**改動：** 給 `_line_svg` 加一個 `empty_msg="無資料"` 參數,把硬編碼字串改為 `{empty_msg}`；`make_eff_svg` 移除 `if not eff: return f'<svg...填寫里程後顯示...></svg>'` 三行,在現有的 `_line_svg(...)` 呼叫尾端加 `empty_msg="填寫里程後顯示"`。`generate_static.py` 從 437 行縮短至 435 行（−2 行)、26339 bytes 縮短至 26217 bytes（−122 bytes）。
**效果：**
- 「空資料 placeholder」的 SVG 結構（viewBox / 字級 / 顏色 / 對齊）現在真正只在 `_line_svg` 一處,任何想換空狀態樣式的需求都改一處,不再有第二個獨立寫死的 placeholder SVG 字串需要同步維護。
- 未來新增圖表 wrapper（譬如健康/餐廳分頁的 SVG）若需要客製化空狀態文案,從「自己手寫一段 23-行 placeholder SVG」縮短為「呼叫 `_line_svg(..., empty_msg="自訂文案")`」。
- 移除了「為何 `make_eff_svg` 是唯一還保留提前返回的 wrapper」這個非對稱認知負擔——三個 svg wrapper 現在都只是單行 `_line_svg(...)` 呼叫,沒有特例。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff <(grep -v 更新於 before.html) <(grep -v 更新於 after.html)` 驗證,99,270 bytes → 99,270 bytes,差異為空）。原因:`make_eff_svg` 過去的早返路徑要求 `eff == []`,即所有 records 都沒有 `km`；但 `DATA["sienna"]` 4 筆有 km、`DATA["c300"]` 1 筆有 km,該分支從未觸發。`_line_svg` 自身的 n==0 分支也從未觸發（所有資料源非空)。即便兩條分支實際被觸發,新版本只會把空 SVG 的 `viewBox` 從 `0 0 340 110` 改為 `0 0 400 110`,顯示結果是同一段「無資料」訊息文字置中於略寬的 SVG 框內,視覺上幾乎不可區分。

---

## 2026-05-04（第二輪 · 晚間排程）
**思考面向：** HTML 產生邏輯（CSS 規則）
**發現：** `build_html` 內嵌 CSS 第 380 行 `body:has(#gas-c300:target) .nav a[href="#gas"]    { color:{C_TEXT1}; border-bottom-color:{C_BLUE}; }` 是 100% 冗餘的「同值再覆蓋一次」防禦。trace 規則：(a) 第 373 行 `.nav a[href="#gas"] { color:{C_TEXT1}; border-bottom-color:{C_BLUE}; }` 是**預設**高亮 gas 連結，永遠生效；(b) 第 374-376 行 `body:has(#grocery:target|...) .nav a[href="#gas"] { ...transparent }` 只在 grocery/dining/other 被 target 時取消 gas 高亮——`#gas-c300` **不在**這個取消列表裡；(c) 因此當 c300 被 target 時，gas 連結本就保持預設高亮，第 380 行只是把它再設一次相同值，毫無觀察效果。這是與 05-01 移除 `make_price_svg`/`make_spending_svg` 冗餘提前返回**同性質**的「雙軌同值防禦」——CSS 版本，先前重構鏈條都聚焦在 Python 端，CSS 是上輪盲點。
**改動：** 從 `build_html` `<style>` 區塊刪除第 380 行那條 `body:has(#gas-c300:target) ...` 規則（單行）。`generate_static.py` 從 26042 bytes 縮短至 25939 bytes（−103 bytes、−1 行）；`index.html` 從 91644 bytes 縮短至 91546 bytes（−98 bytes，純粹是該行 CSS 字串本身）。
**效果：**
- 「gas 連結高亮邏輯」現在真正集中於兩條規則（第 373 行預設啟用 + 第 374-376 行取消列表），不再有第三條「c300 重新啟用」的雙軌防禦；CSS 級聯邏輯回到「預設+取消」這個最簡單的兩段式心智模型。
- 未來若再新增分頁（譬如「健康」「訂閱」），照「在第 374-376 行加進取消列表 + 加一條自身高亮」這個既有 pattern 抄就好，**不需要**再為 `gas-c300` 維護一條重複的「重新啟用 gas」規則——因為根本不存在這需求。
- 移除了「為何 grocery/dining/other 在第 374-376 取消、377-379 重啟自身，但 c300 卻在第 380 重啟 gas（不是自己）」這個非對稱認知負擔；現在 5 個分頁的高亮規則完全對稱：`#gas` 預設亮 → 在 grocery/dining/other 取消；`#grocery`/`#dining`/`#other` 預設不亮 → 各自被 target 時亮起；`#gas-c300` 不需要管 nav，因它本來就屬於 gas 分頁。
- HTML 輸出的「視覺渲染」byte-for-byte 完全相同（已用 `diff <(grep -v 更新於 before.html) <(grep -v 更新於 after.html)` 驗證，唯一差異就是這條被刪掉的 CSS 字串）；任何瀏覽器跑這份新 HTML，所有 hash 路由（#gas、#gas-c300、#grocery、#dining、#other、空 hash）下 nav 高亮狀態與舊版完全一致。

---

## 2026-05-04
**思考面向：** HTML 產生邏輯
**發現：** `make_grocery_card`（行 266-270）與 `make_other_card`（行 276-279）底部都有一個「flex 列 + N 個 label-value 對」的 meta 行，使用**完全相同**的內聯樣式：外層 `<div style="display:flex;gap:16px;flex-wrap:wrap;padding-top:Xpx;border-top:1px solid {C_BORDER}">` + 每對 `<div><span style="font-size:10px;...uppercase;...">label </span><span style="font-size:12px;color:{C_TEXT2}">value</span></div>`。差異只有 `padding-top`（8px vs 10px）與 pair 數量。一段 `font-size:10px;color:{C_TEXT3};text-transform:uppercase;letter-spacing:0.08em` 與 `font-size:12px;color:{C_TEXT2}` 的「小標籤+值」span 樣式串，分散在 5 個 inline div 重複出現（grocery 3 行 + other 2 行）。這是 05-01 PM `card_header` 重構鏈條後**仍未統一的最後一處 inline 樣式雙胞胎**。
**改動：** 抽出 `meta_row(pairs, padding_top="10px")` 共用函式（8 行）。`pairs` 是 `(label, value)` tuple list；`padding_top` 用參數保留 grocery 8px / other 10px 的視覺差異。`make_grocery_card` 5 行 inline meta div → 一行 `meta_row([("小計", f"${...:.2f}"), ("HST", ...), ("付款", ...)], "8px")`；`make_other_card` 4 行 → 一行 `meta_row([("類別", ...), ("付款", ...)])`。git diff 顯示 +10 / −9（淨 +1 行），但 inline span 樣式串實例數從 5 縮到 1（−80%）。
**效果：**
- 未來新增清單型卡片（譬如「健康」「訂閱」「餐廳」分頁的條目卡）需要底部 meta 列時，從「複製 4-5 行 inline div + span 並小心同步 4 個 hard-coded style 字串」縮短為「呼叫 `meta_row([(label, value), ...])` 一行」。
- 改 meta 行樣式（label 字級、值字級、letter-spacing、padding、邊框、gap）時，從「2 個函式 × 5 個 inline span 各改一次（極易漏改 → grocery / other 看起來不一樣）」縮短為「改 `meta_row` 一處」，所有底部 meta 行真正集中於單一函式。
- 移除了「`make_grocery_card` 用 `padding-top:8px` 但 `make_other_card` 用 `padding-top:10px` 卻其餘字串完全一樣」這個 inconsistency 認知負擔——padding 差異現在用函式參數明確表達，不再淹沒在 5 行幾乎一樣的 inline style 之中。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff <(grep -v 更新於 before) <(grep -v 更新於 after)` 驗證，91,644 → 91,644 bytes，唯一差異是時間戳）。

---

## 2026-05-01（第二輪 · 晚間排程）
**思考面向：** HTML 產生邏輯
**發現：** `make_record_card`（行 174-184）、`make_grocery_card`（行 246-256）、`make_other_card`（行 267-277）三個 card 工廠都以**完全相同**的結構開頭——同一個 outer wrapper（白底/邊框/圓角 padding:16px margin-bottom:10px）+ 同一個 flex header（左側 title+subtitle、右側 ${total} + date·time）。共 ~11 行 HTML × 3 處 = 33 行重複，只在 5 個欄位名（`station`/`store`/`desc`、`addr`/`addr`/`note`）與 1 個 inline style 微差（`make_other_card` 的 subtitle 多 `;line-height:1.5`，因 `note` 可能換行）上不同。這是 04-24 `chart_block` → 04-27 `empty_state` → 04-29 兩輪 `make_car_section` / `make_list_section` 重構鏈條上**最後一處仍未統一的雙胞胎**。
**改動：** 抽出 `card_header(title, subtitle, total, date, time, subtitle_extra="")` 共用函式（14 行）。三個 card 工廠的「outer wrapper + header」11 行各被替換成單行 `card_header(...)` 呼叫；`subtitle_extra` 預設空字串、`make_other_card` 傳 `';line-height:1.5'` 保留 `note` 的多行可讀性。`generate_static.py` 從 439 行縮短至 420 行（−19 行）。
**效果：**
- 未來新增 card 類型（譬如「健康」「訂閱」分頁的條目卡）時，從「複製 11 行 outer+header HTML 並小心改 4 個變數」縮短為「呼叫 `card_header(title, sub, total, date, time)` 一行 + 自己的 body + `</div>`」。
- 改卡片頭樣式（背景色、圓角、padding、總額顏色、字級）時，從「3 個函式各改一次（容易漏改一處導致 grocery / record / other 三種卡長得不一樣）」縮短為「改 `card_header` 一處」，所有卡片頭真正集中於單一函式。
- 移除了「為何 `make_record_card` 用 `r['station']`、`make_grocery_card` 用 `r['store']`、`make_other_card` 用 `r['desc']` 但 outer+header 各重抄一次相同字串」的 inconsistency 認知負擔——資料欄位差異現在用函式參數明確表達，不再淹沒在 11 行 inline style 之中。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff` 驗證,87,127 → 87,127 bytes,唯一差異是時間戳）。

---

## 2026-05-01
**思考面向：** 圖表函式
**發現：** `make_price_svg`（行 107-111）與 `make_spending_svg`（行 121-126）在開頭都有 `if not records: return _line_svg([], [], ...)` 提前返回。但檢查 `_line_svg` 第 79-80 行——它已經自己處理 `n == 0` 並回傳「無資料」placeholder。把空 `records` 直接餵給後面的 list comprehension 會得到 `labels=[]` / `vals=[]`，呼叫 `_line_svg([], [], ...)` 結果**完全相同**。這兩個提前返回是 100% 冗餘。額外發現：`make_price_svg` 的提前返回行用了多餘的 f-string 字面值 `f"rgba(0,113,227,0.08)"`（沒有插值卻寫 `f""`），與其他呼叫處不一致。`make_eff_svg` 不算（它的提前返回用了自訂訊息「填寫里程後顯示」，有實際語意）。
**改動：** 移除 `make_price_svg` 與 `make_spending_svg` 兩處冗餘提前返回。`generate_static.py` 從 25457 bytes 縮短至 25256 bytes（−201 bytes、−2 行）。
**效果：**
- 「空資料 placeholder」這個語意現在真正集中於 `_line_svg` 一處，不再有 `make_price_svg` 與 `_line_svg` **同步**處理空值的雙軌防禦。
- 未來新增類似圖表 wrapper（譬如健康/餐廳分頁的支出圖）時，從「記得加 `if not records: return _line_svg([], [], ...)` 防呆」縮短為「直接寫 list comprehension，空 list 自動穿透到 `_line_svg`」。
- 移除了「為何 `make_price_svg` 提前返回用 `f""` 但實際路徑不用」這個 inconsistency 認知負擔。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff` 驗證，唯一差異是時間戳）——因為 `DATA["sienna"]`、`DATA["c300"]`、`GROCERY`、`OTHER` 目前都非空，這兩個分支永不執行；移除後若未來真的傳入空 list，`_line_svg` 的 n==0 分支會接住它。

---

## 2026-04-29（第二輪 · 晚間排程）
**思考面向：** HTML 產生邏輯
**發現：** `make_grocery_section`（行 294-307）與 `make_other_section`（行 279-291）是**同一個函式的雙胞胎**——相同的 if-empty 提前返回、相同的 `total_spent = sum(r["total"]…)`、相同的 `reversed()` 反序、相同的 `chart_block("每筆支出趨勢  CAD", make_spending_svg(...))`、相同的 grid-template-columns CSS、相同的 stat_card 兩格佈局、相同的「筆記錄」label 與 list-label inline style 字串。**只有 5 個變數差異**：資料來源（`OTHER`/`GROCERY`）、empty label、card 工廠、count unit（「筆」/「次」）、list label。這是 04-24 `chart_block` → 04-27 `empty_state` → 04-29 AM `make_car_section` 重構鏈條上**最後一處仍未統一的雙胞胎**。
**改動：** 抽出 `make_list_section(records, empty_label, card_fn, count_unit, list_label)` 共用函式（11 行），兩個重複函式被替換成單一函式 + 兩個呼叫點。`generate_static.py` 從 446 行縮短至 431 行（−15 行）、25707 bytes 縮短至 25180 bytes（−527 bytes）。
**效果：**
- 未來新增分頁類型（譬如「健康」「娛樂」「訂閱」）時，從「複製 ~14 行 section 函式並小心改 5 處變數」縮短為「呼叫 `make_list_section(HEALTH, "健康", make_health_card, "筆", "醫療記錄")` 一行」。
- 改 section 樣式（grid 欄數、margin、stat_card 排列）時，從「2 個地方各改一次（容易漏改）」縮短為「改 1 處」，所有清單型 section 真正集中於單一函式。
- 移除了「`make_grocery_section` 用 `total_tx` 變數、`make_other_section` 直接用 `len(OTHER)`」這個 inconsistency 認知負擔。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff` 驗證，唯一差異是時間戳）。

---

## 2026-04-29
**思考面向：** HTML 產生邏輯
**發現：** 04-27 抽離出 `empty_state(icon, label)` 共用函式後，已套用於 grocery / dining / other 三處空狀態。但 `make_car_section`（第 159-167 行）車輛記錄為空時，**仍保留第 4 處內聯空狀態 HTML**（7 行 inline div + 寫死 inline style + 多餘的 `icon = "🚗"` 區域變數），完全沒換成 `empty_state` 函式 — 上輪重構遺漏的最後一處。雙軌並存，違反 DRY。
**改動：** 把 `make_car_section` 空狀態分支從 7 行內聯 HTML 縮為一行 `return empty_state("🚗", car_label)`。`generate_static.py` 從 387 行縮短至 381 行（-6 行）。
**效果：**
- 未來改空狀態樣式（hint 文案、padding、顏色）時，從「3 個函式呼叫 + 1 處 inline HTML 各改一次（容易漏改）」縮短為「改 `empty_state` 一處」，所有空狀態真正集中於單一函式。
- 移除了「為何只有車輛區段用 inline 空狀態、其他都用 `empty_state`」這個 inconsistency 認知負擔。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff` 驗證，唯一差異是時間戳）— 因為 `DATA["sienna"]` 12 筆、`DATA["c300"]` 1 筆，這個分支永不執行。

---

## 2026-04-27（第二輪 · 晚間排程）
**思考面向：** HTML 產生邏輯
**發現：** 三個空狀態 HTML（`make_grocery_section` line 229、`build_html` 餐廳 line 333、其他 line 338）以**完全相同**的字串模式重複出現，僅 icon（🛒/🍽/📦）與 label（超市/餐廳/其他）不同。每處 ~200 bytes 內聯 HTML，三處共 ~600 bytes 重複。
**改動：** 抽出 `empty_state(icon, label)` 共用函式（4 行），三個重複字串改為單行函式呼叫。`generate_static.py` 從 20077 bytes 縮短至 19906 bytes（-171 bytes），淨削減 3 處 inline HTML。
**效果：**
- 未來新增空狀態類型（譬如「健康」「娛樂」分頁）時，從「複製 ~200 bytes 內聯 HTML 並小心改 emoji/label」縮短為「呼叫 `empty_state("🏥", "健康")` 一行」。
- 改空狀態樣式（hint 文案、顏色、字級）時，從「3 個地方各改一次（容易漏改）」縮短為「改 1 處」。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff` 驗證，唯一差異是時間戳）。

---

## 2026-04-27
**思考面向：** 資料結構
**發現：** `GROCERY` 與 `DATA` 中每筆記錄都帶有 `"id":N` 欄位（共 17 筆 = 4 grocery + 11 sienna + 1 c300），但 grep 全檔案後確認 **`id` 在程式碼中從未被讀取**（沒有 `r["id"]`、`.get("id")`、或任何引用）。更糟的是，這些 ID **不唯一**：grocery 用 1-4、sienna 用 1-11（其中 1-4 與 grocery 重複）、c300 用 1（與 sienna、grocery 都重複）。純粹的雜訊欄位，每筆新收據都得多寫一個沒有用、還會搞混的編號。
**改動：** 用 `sed -E 's/"id":[0-9]+, +//g'` 一次移除所有 17 筆記錄的 `id` 欄位。`generate_static.py` 從 20217 bytes 縮短至 20077 bytes（-140 bytes）。
**效果：**
- 未來 receipt-processor 新增收據時，從「決定一個 id 編號（還得查目前最大值避免衝突）+ 寫進資料」縮短為「直接寫進資料」，每筆記錄少一欄。
- 移除了「ID 重複但程式照樣運作」這個誤導性陷阱——以後若真要加唯一識別碼，就乾乾淨淨地從零設計，不會被舊的假 ID 干擾。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff` 驗證，唯一差異是時間戳）。

---

## 2026-04-25
**思考面向：** 圖表函式
**發現：** `_line_svg` 簽章有 `color_dot` 參數，但檢查 5 個呼叫點（`make_price_svg` x2、`make_eff_svg` x1、`make_spending_svg` x2，含空白 placeholder），`color_dot` **永遠等於 `color_line`**。100% 冗餘參數，每個呼叫點都重複指定同一個十六進位色碼字串。
**改動：** 從 `_line_svg` 簽章移除 `color_dot`；資料點 `<circle>` 直接用 `color_line` 上色。5 個呼叫點各刪除一個冗餘色碼字串。`generate_static.py` 從 20282 bytes 縮短至 20217 bytes（-65 bytes）。
**效果：**
- 未來新增圖表時，從「指定 4 個顏色（line/fill/dot/label）」縮短為「指定 3 個顏色（line/fill/label）」，少一個容易寫錯的同色字串。
- 改主色時，從「同步改 line + dot 兩處（容易漏改）」縮短為「改 1 處」。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff` 驗證，唯一差異是時間戳）。

---

## 2026-04-24
**思考面向：** HTML 產生邏輯
**發現：** `make_car_section` 與 `make_grocery_section` 中，圖表外框 HTML（含 `margin-bottom`、標題列、白色卡片背景）以**完全相同**的內聯樣式重複出現 4 次（油價、油耗、加油花費、超市支出趨勢）。每個區塊 4 行 HTML × 4 處 = 16 行重複。
**改動：** 抽出 `chart_block(title, svg)` 共用函式，4 個重複區塊改為單行函式呼叫。`generate_static.py` 從 363 行縮短至 357 行（-6 行）、20803 bytes 縮短至 20282 bytes（-521 bytes）。
**效果：**
- 未來新增圖表時，從「複製 4 行 HTML 並小心改 emoji/標題/變數」縮短為「呼叫 `chart_block("emoji 標題", svg)` 一行」。
- 改外框樣式（背景色、圓角、padding）時，從「4 個地方各改一次（容易漏改）」縮短為「改 1 處」。
- HTML 輸出 byte-for-byte 完全相同（已用 `diff` 驗證，唯一差異是時間戳）。

---

---

## 2026-05-02
**工作摘要：**

### UI 全面重新設計
**問題：** 頁面風格太「卡通」，顏色過重（深藍黑底、黃色橘色大字、emoji 到處都是），與 Apple/MUJI 極簡風格相差甚遠。
**改動：**
- 背景改為 Apple 淺色系 `#f5f5f7`，卡片純白 `#ffffff`，邊框改為 `rgba(0,0,0,0.08)` 超細透明線
- 強調色換成 Apple 系統色：藍 `#0071e3`（油價/PC點）、深綠 `#1a8c3e`（油耗/PC餘額）、深橙 `#c45000`（金額）
- 字型改用 `-apple-system` SF Pro，標籤全大寫 + letter-spacing，數字大清晰
- 全面移除 emoji，導航列、卡片、圖表標題均為純文字
- Header/Nav 加入毛玻璃效果（`backdrop-filter`）
- 新增 `html { background:#f5f5f7 }` 防止底部露出白色以外的底色
- 抽出顏色常數（`C_BG`、`C_SURFACE`、`C_BLUE` 等），未來改色只需改頂部常數

### Bug 修復：收據排程「分類照片但不寫資料」
**問題：** 昨晚排程掃到 3 張新收據（IMG_4307、IMG_4322、IMG_4323），照片正確移入 `超市/` 子資料夾，但 `generate_static.py` 完全沒有寫入任何新資料。根本原因：Step 4（Edit 寫入）靜默失敗後，任務未中止，繼續執行 Step 6（移動照片），導致「照片走了、資料沒進去」。

**修正：**
- 在 Step 4 之後加入**強制驗證**：用 `grep` 確認剛寫入的日期出現在檔案中
- 驗證失敗 → 立刻停止，不執行 Step 5/6/7，回報錯誤
- **Step 6（移動照片）只能在 Step 4+5 皆成功後才執行**
- 原則：照片移走 = 資料一定已寫入，兩者永遠同步

**補救：** 手動讀取三張照片，補入缺失資料：
- 莊記 2026-05-01 綜合零食發票 $17.79（2頁）
- FreshPro Foodmart 2026-05-01 走地雞蛋 x2 $9.98

### 新增收據
- 莊記 2025-11-14 板麻 XO 魷魚醬 250g $13.00（使用者手動提供照片）

